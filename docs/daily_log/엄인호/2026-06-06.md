# 2026-06-06 작업 내역

## 오늘 반영된 주요 작업 요약

마이페이지 프로필 사진 변경 기능 구현, 컴포넌트 50줄 이내 분리 리팩토링, 인증 가드 적용을 진행했습니다.

---

## 세부 작업

### 1. 프로필 사진 변경 기능 구현

- `store/authStore.ts` — `patchUser(patch: Partial<AuthUser>)` 메서드 추가, 업로드 후 스토어 즉시 갱신
- `app/api/user/avatar/route.ts` — Supabase Storage `avatars` 버킷 업로드 POST 엔드포인트 신규 생성
- `hooks/user/useAvatarUpload.ts` — 파일 선택 → 미리보기 → 업로드 로직 훅으로 분리
- `components/(auth)/mypage/profile/ProfileAvatar.tsx` — 파일 인풋, 미리보기 이미지, 업로드 버튼 UI

### 2. 개인정보 수정 폼 상태 훅 분리

- `hooks/user/useProfileEditForm.ts` — ProfileEdit 의 상태·핸들러·submit 로직 전체를 커스텀 훅으로 추출
- `components/(auth)/mypage/profile/ProfileEdit.tsx` — 훅 사용 + JSX 렌더링만 담당

### 3. 프로필 API 비밀번호 변경 로직 분리

- `app/api/auth/profile/changePassword.ts` — 소셜 여부 확인 + 현재 비밀번호 검증 + 변경 로직 헬퍼로 추출
- `app/api/auth/profile/route.ts` — `changePassword` 헬퍼 임포트로 PATCH 핸들러 50줄 이내 유지

### 4. 마이페이지 인증 가드 적용

- `components/(auth)/mypage/MypageGuard.tsx` — Zustand `authStore` 수화 완료 후 `accessToken` 없으면 `/login` 으로 리다이렉트하는 클라이언트 가드 컴포넌트 신규 생성
- `app/(main)/mypage/layout.tsx` — `MypageGuard` 로 `UserCard` 래핑, 미인증 접근 차단

---

## 커밋

| 해시      | 내용                                                         |
| --------- | ------------------------------------------------------------ |
| `161f2d2` | feat: 마이페이지 프로필 사진 변경 기능 구현                  |
| `6888153` | refactor: 마이페이지 컴포넌트 50줄 이내 분리                 |
| `3c3c4f6` | docs: 2026-06-06 daily log 작성                              |
| `38f53ea` | feat: 마이페이지 인증 가드 컴포넌트 추가 및 레이아웃 적용    |
