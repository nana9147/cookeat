# 2026-06-16 작업 내역

## 오늘 반영된 주요 작업 요약

관리자 상품 관리 페이지를 신규 구현하고, 목업 데이터를 제거하여 실 API로 연동했습니다. 회원·판매자 관리 페이지의 검색이 현재 페이지 데이터만 필터링하는 버그를 발견하여 서버 사이드 검색으로 교체했습니다. 이후 상품 관리 페이지에 페이지네이션을 추가했습니다.

---

## 세부 작업

### 1. 관리자 상품 관리 페이지 신규 구현

- `app/admin/products/page.tsx` (신규)
  - 상품 목록 테이블, 상세보기·수정·삭제 다이얼로그 UI 구현
  - 상태·카테고리 필터 및 판매자명·상품명 검색 기능 구현

### 2. 상품 관리 페이지 실 API 연동

- `app/admin/products/page.tsx`
  - 목업 데이터 배열 제거, `GET /api/admin/products` 실 API 연동
  - 검색어 입력 시 400ms 디바운스 후 API 호출 (`useRef` + `setTimeout` 패턴)
  - 필터·검색 변경 시 이전 요청이 늦게 응답해도 화면을 덮어쓰지 않도록 `cancelled` 플래그로 경쟁 상태 방지
  - 수정 저장: `PATCH /api/admin/products/:id` 호출 후 로컬 state 동기화
  - 삭제: `DELETE /api/admin/products/:id` 호출 후 목록과 전체 카운트 갱신
- `components/common/StatusBadge.tsx`
  - `숨김` 상태 스타일 추가 (API 실제 상태값 반영)

### 3. 회원·판매자 관리 페이지 검색 버그 수정

- `app/admin/members/page.tsx`, `app/admin/sellers/page.tsx`
  - 기존: `const filtered = list.filter(...)` — 현재 페이지 20건만 클라이언트에서 필터링하여 2페이지 이후 데이터는 검색 불가한 버그
  - 변경: `keyword` 파라미터를 API에 전달하는 서버 사이드 검색으로 교체
  - 검색어 변경 시 400ms 디바운스 적용 및 1페이지로 자동 리셋

### 4. 상품 관리 페이지 페이지네이션 추가

- `app/admin/products/page.tsx`
  - `page` state 추가, API 호출 시 `page`·`limit(20)` 파라미터 전달
  - 필터·검색 변경 시 1페이지로 리셋
  - 공통 `Pagination` 컴포넌트 적용 (회원·판매자 페이지와 동일 구조)
  - "N개 중 1–20개" 카운트 표시 추가

---

## 커밋

| 해시      | 내용                                          |
| --------- | --------------------------------------------- |
| `b9f4c73` | feat: 관리자 페이지 상품 관리 라우트          |
| `409c1a3` | feat: 숨김 상태 추가                          |
| `1ca1d38` | fix: 관리자 회원·판매자 검색을 서버 사이드로 교체 |
| `6634999` | feat: 관리자 상품 관리 페이지 실 API 연동     |
