# 2026-06-11 작업 내역

## 오늘 반영된 주요 작업 요약

판매자·상품·주문 관리 필터 및 검색 기능 추가, 리뷰 신고 페이지 리팩터링, 관리자 대시보드 실데이터 연동 및 API 라우트 구현을 진행했습니다.

---

## 세부 작업

### 1. 판매자 관리 필터 추가

- `app/admin/sellers/page.tsx`
  - 상태·승인 여부 필터 패널 추가 (`showFilter` 상태로 토글)
  - 활성 필터 개수 뱃지 표시
  - 검색어 + 필터 조건 AND 결합 `filtered` 로직 구현
  - shadcn `Select` 빈 값 미지원 이슈 대응 — 전체 옵션 값을 `'all'`로 처리

### 2. 상품 관리 필터 및 검색 추가

- `app/admin/products/page.tsx`
  - 카테고리·상태 필터 패널 추가
  - 상품명 텍스트 검색과 필터 조건 AND 결합
  - 활성 필터 개수 뱃지 표시

### 3. 주문 관리 필터 및 검색 추가

- `app/admin/orders/page.tsx`
  - 주문 상태 필터 패널 추가 (`'결제완료'` / `'주문확인'` / `'배송준비'` / `'배송중'` / `'배송완료'` / `'취소'`)
  - 주문번호·사용자 텍스트 검색과 필터 조건 AND 결합

### 4. 리뷰 신고 페이지 별점 코드 리팩터링

- `app/admin/reviews/page.tsx`
  - 별점 렌더링 로직을 판매자 관리 페이지와 동일한 방식으로 통일
  - 불필요한 스타일 및 중복 코드 제거

### 5. 관리자 대시보드 실데이터 연동 및 API 라우트 추가

- `app/api/admin/dashboard/route.ts` (신규)
  - `requireAdmin`으로 관리자 인증 가드 적용
  - Supabase `supabaseAdmin`으로 다음 데이터를 `Promise.all` 병렬 조회
    - 오늘 총 매출 (`orders.final_amount` 합계)
    - 오늘 주문 건수 / 취소 건수
    - 오늘 신규 회원 수 / 판매자 수
    - 오늘 문의 건수
    - 인기 상품 Top 4 (`sales_count` 내림차순)
- `app/admin/page.tsx`
  - `'use client'` 전환, `useEffect`로 `/admin/dashboard` API 호출
  - 하드코딩된 `statCards` / `popularProducts` / `categoryStats`를 API 응답 실데이터로 교체
  - `StatCard`, `PopularProduct`, `CategoryStat` 인터페이스 정의
  - 트렌드 색상 클래스를 `text-green-600` → `text-primary` / `text-red`로 통일

---

## 커밋

| 해시      | 내용                                              |
| --------- | ------------------------------------------------- |
| `81b6690` | feat: 판매자 관리 필터 추가                       |
| `346ce5a` | refactor: 리뷰 신고 페이지 별점 판매자 코드와 동일하게 변경 |
| `649f4a7` | feat: 상품 관리 필터 및 검색 추가                 |
| `d40ea1d` | fix: 검색 placeholder 페이지에 맞게 변경          |
| `7d9076a` | refactor: 필요없는 스타일 제거                    |
| `680a23d` | fix: 필터 버튼 필요없는 코드 제거                 |
| `2a2e06c` | feat: 주문관리 필터 및 검색 추가                  |
| `cd0e8bf` | feat: 관리자 대시보드 실데이터 연동 및 API 라우트 추가 |
