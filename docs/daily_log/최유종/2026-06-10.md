# 2026-06-10 작업 내역

## 오늘 반영된 주요 작업 요약

판매자 관리 상세보기·수정·삭제 구현, 어드민 회원/판매자 수 동적 표시 수정, 상품 관리 상세보기·수정·삭제 구현, 주문 목록 상세보기 다이얼로그 추가를 진행했습니다.

---

## 세부 작업

### 1. 판매자 관리 상세정보 및 수정·삭제 기능 구현

- `app/admin/sellers/page.tsx`
  - `Seller` 인터페이스에 `address`, `bankName`, `bankAccount` 필드 추가
  - `Status` 타입에 `'거절'`, `'정지'` 추가 및 배지 색상 맵 보완 (`대기` → `bg-yellow`)
  - `sellerList`, `selectedSeller`, `editSeller` 상태 추가
  - 상세보기(`Eye`) 클릭 → `handleViewDetail` → 주소·은행 정보 포함 읽기 전용 다이얼로그 표시
  - 수정(`Pencil`) 클릭 → `handleEdit` → `Status` `Select`로 상태 변경, 저장 시 인플레이스 수정
  - 삭제(`Trash`) 클릭 → `setSellerList`로 해당 id 제거

### 2. 어드민 회원/판매자 수 하드코딩 제거

- `app/admin/members/page.tsx`, `app/admin/sellers/page.tsx`
  - `'1,234명'` 등 하드코딩된 수치를 `{memberList.length}명` / `{sellerList.length}명` 으로 변경하여 실제 목록 길이와 동기화

### 3. 상품 관리 상세정보 및 수정·삭제 기능 구현

- `app/admin/products/page.tsx`
  - `Status` 타입에 `'판매중지'` 추가
  - `CATEGORIES` 상수(`['채소', '과일', '육류', '수산물', '유제품', '가공식품', '기타']`) 정의
  - `productList`, `selectedProduct`, `editProduct` 상태 추가
  - 상세보기 다이얼로그: 상품명·판매자·카테고리·가격·재고·상태 읽기 전용 표시
  - 수정 다이얼로그: 카테고리·상태를 `Select`로 변경, 저장 시 스프레드 연산자로 인플레이스 수정
  - 삭제: `setProductList`로 해당 id 제거
  - 컴포넌트명 오타 수정 (`MembersPage` → `ProductsPage`)

### 4. 주문 목록 상세보기 다이얼로그 추가

- `app/admin/orders/page.tsx`
  - `OrderItem`, `Shipping`, `Order` 인터페이스 정의
    - `Order`: 주문번호·사용자·금액·결제수단·수령인·주소·요청사항 등 실 DB 구조 반영
    - `OrderItem`: 상품ID·수량·단가 포함
    - `Shipping`: 택배사·운송장번호·발송일·배달완료일 포함
  - `Status` 타입 확장(`'결제완료'` / `'주문확인'` / `'배송준비'` / `'배송중'` / `'배송완료'` / `'취소'`)
  - 상세보기(`Eye`) 클릭 → 주문 정보·배송 정보·주문 상품 목록을 섹션별로 보여주는 다이얼로그 구현

### 5. 회원 관리 등급·상태 필터 기능 구현

- `app/admin/members/page.tsx`
  - `showFilter`, `filterGrade`, `filterStatus` 상태 추가
  - 필터 버튼 클릭 시 패널 토글, 활성 필터 개수 뱃지 표시
  - shadcn `Select`로 등급(`전체` / `일반` / `VIP`)·상태(`전체` / `정상` / `정지`) 선택 UI 구현
  - `filtered` 로직을 검색어 + 등급 + 상태 세 조건 AND 결합으로 확장
  - shadcn `Select`의 빈 문자열 미지원 이슈 대응 — 전체 옵션 값을 `'all'`로 처리

---

## 커밋

| 해시      | 내용                                            |
| --------- | ----------------------------------------------- |
| `5ab7c9e` | feat: 판매자 관리 상세정보 및 수정, 삭제 추가   |
| `a2481c7` | fix: 어드민 회원/판매자 수 하드코딩 변경        |
| `52c043d` | feat: 상품관리 상세정보 및 수정 삭제 추가       |
| `f55b78a` | feat: 관리자 주문 목록 상세보기 다이얼로그 추가 |
