# 2026-06-04 작업 내역

## 오늘 반영된 주요 작업 요약

어드민 전용 레이아웃 분리, 사이드바 컴포넌트 구현, 대시보드 통계 UI 구현을 진행했습니다.

---

## 세부 작업

### 1. 어드민 페이지 사이드바 추가

- shadcn/ui `sidebar` 컴포넌트 설치 및 관련 UI 파일 추가
  - `components/ui/sidebar.tsx` — shadcn Sidebar 기반 컴포넌트
  - `components/ui/sheet.tsx` — 모바일 드로어용 Sheet 컴포넌트
  - `components/ui/input.tsx`, `separator.tsx`, `skeleton.tsx`, `tooltip.tsx` — Sidebar 의존 UI
  - `hooks/use-mobile.ts` — 모바일 감지 훅
- `app/admin/layout.tsx` — `SidebarProvider` 래핑 및 어드민 레이아웃 초안 작성
- `app/admin/page.tsx` — 어드민 대시보드 페이지 초안 작성

### 2. 어드민 레이아웃 분리를 위한 라우트 그룹 (main) 적용

- 일반 사용자 페이지(홈/게시판/마이페이지/레시피)를 `app/(main)/` 라우트 그룹으로 이동
  - 헤더·푸터가 포함된 공통 레이아웃 `app/(main)/layout.tsx` 추가
  - 어드민(`/admin`) 영역은 별도 레이아웃을 유지해 사용자 영역 레이아웃과 완전 분리
- `app/layout.tsx`에서 어드민 무관 레이아웃 코드 정리
- `app/admin/components/Header/Header.tsx` — 어드민 전용 헤더 컴포넌트 추가

### 3. 어드민 대시보드 통계 UI 및 사이드바 컴포넌트 구현

- `app/admin/components/Sidebar/AdminSidebar.tsx` — 데스크탑용 사이드바
  - shadcn Sidebar 기반, 메뉴 항목 11개 (대시보드 / 회원관리 / 판매자관리 등)
  - 선택된 메뉴에 `bg-primary text-white` 활성 스타일 적용
  - `md` 이하 해상도에서 `hidden` 처리
- `app/admin/components/Sidebar/AdminMobileNav.tsx` — 모바일용 드롭다운 네비게이션
  - 현재 선택 메뉴를 버튼으로 노출, 클릭 시 전체 메뉴 드롭다운 토글
  - `md` 이상 해상도에서 `hidden` 처리
- `app/admin/page.tsx` — 대시보드 통계 UI
  - 통계 카드 6종 (오늘 총 매출 / 주문 건수 / 신규 가입자 / 판매자 가입 / 취소·환불 / 문의 건수), 증감 트렌드 표시
  - shadcn Card + Progress 컴포넌트로 인기 상품 순위 및 카테고리별 매출 섹션 구현
  - `components/ui/card.tsx`, `progress.tsx` 추가
- `app/admin/layout.tsx` — 최종 어드민 레이아웃 정리 (Header + SidebarProvider + AdminSidebar + AdminMobileNav)

### 4. 사이드바 실제 라우팅 연결

- `AdminSidebar.tsx`, `AdminMobileNav.tsx` 공통 개선
  - 메뉴 항목을 문자열 배열에서 `{ label, href }` 객체 배열로 변경
  - `useState`로 관리하던 활성 메뉴를 `usePathname()`으로 교체 → 새로고침·직접 URL 진입 시에도 올바른 활성 상태 유지
  - `SidebarMenuButton`에 `asChild` + `<Link>` 조합 적용 (데스크탑)
  - `AdminMobileNav` 메뉴 항목을 `<button>` → `<Link>`로 교체, 클릭 시 드롭다운 자동 닫힘 유지

### 5. 회원관리 페이지 구현

- `app/admin/members/page.tsx` — 회원 목록 관리 UI
  - 이름·이메일 실시간 검색 필터
  - 필터 버튼 (UI 배치, 기능 확장 예정)
  - shadcn Table 기반 회원 테이블 (회원명 / 이메일 / 가입일 / 등급 / 주문수 / 상태 / 관리)
  - 등급 배지: `일반(beige)` / `VIP(yellow)`, 상태 배지: `정상(primary)` / `정지(red)`
  - 행별 관리 버튼: 상세보기(Eye) / 수정(Pencil) / 정지(Ban)
- `components/ui/table.tsx` — shadcn Table 컴포넌트 추가 (회원관리 페이지 의존)

---

## 커밋

| 해시      | 내용                                                          |
| --------- | ------------------------------------------------------------- |
| `c8c5565` | 어드민 페이지 사이드바 추가                                   |
| `20c5b26` | refactor: 어드민 레이아웃 분리를 위한 라우트 그룹 (main) 적용 |
| `16718d4` | feat: 어드민 대시보드 통계 UI 및 사이드바 컴포넌트 추가       |

> **미커밋 작업** (4, 5번): 사이드바 라우팅 개선 및 회원관리 페이지 — 커밋 예정
