# 2026-06-09 작업 내역

## 오늘 반영된 주요 작업 요약

FAQ 추가·수정 다이얼로그 구현, 뱃지 색상 global.css 통일, 통계분석 페이지 신규 추가, 회원 관리 페이지 작업을 진행했습니다.

---

## 세부 작업

### 1. FAQ 추가 및 수정 기능 구현

- `app/admin/support/faq/page.tsx`
  - `Faq` 인터페이스에 `content` 필드 추가
  - `+ FAQ 추가` 버튼 클릭 시 shadcn `Dialog` 열림
    - 카테고리(`Select`), 제목(`Input`), 내용(`Textarea`), 공개 상태(`Select`) 입력 폼
    - 저장 시 목록에 실시간 추가 (`handleAddSave`)
  - 수정 아이콘(`Pencil`) 클릭 시 기존 값 채워진 편집 다이얼로그 열림
    - `handleEditOpen` → `handleEditSave`로 해당 항목 인플레이스 수정

### 2. 뱃지 색상 global.css와 통일

- `app/admin/support/page.tsx`, `app/admin/support/faq/page.tsx`
  - 카테고리 뱃지 중 `환불`(`text-red-700` → `text-red`), `회원`(`text-yellow-700` → `text-yellow`) 색상을 global.css 토큰과 동일하게 수정

### 3. 통계분석 페이지 신규 구현

- `app/admin/analytics/page.tsx` 신규 생성
  - 핵심 지표 카드 4개: GMV(총 거래액) / 전환율 / 재구매율 / MAU
    - `TrendingUp` / `TrendingDown` 아이콘으로 전월 대비 증감 표시
  - 카테고리별 매출 패널
    - shadcn `Progress` 바로 채소·육류·과일·수산·기타 비율 시각화
  - 판매자별 매출 TOP 5 패널
    - 순위(`#N`) + 판매자명 + 매출액 리스트

### 4. 회원 관리 페이지 구현

- `app/admin/members/page.tsx`
  - `Member` 인터페이스 정의 (id, name, email, joinedAt, grade, orderCount, status)
  - 등급(`Grade`: 일반/VIP), 상태(`Status`: 정상/정지) 유니온 타입으로 타입 안전성 확보
  - 등급·상태별 배지 색상 맵 (`gradeBadge`, `statusBadge`) 으로 Tailwind 클래스 관리
  - 검색창: 이름·이메일 실시간 필터링 (`memberList.filter`)
  - 테이블 각 행에 관리 버튼 3개 구현
    - `Eye` (상세보기): `handleViewDetail` → `selectedMember` 상태 저장 → 상세보기 다이얼로그 오픈
    - `Pencil` (수정): `handleEdit` → `editMember` 상태 저장 → 수정 다이얼로그 오픈
    - `Ban` (삭제): 인라인에서 `setMemberList`로 해당 id 제거
  - 상세보기 다이얼로그: 이름·이메일·가입일·등급·주문수·상태 읽기 전용 표시
  - 수정 다이얼로그: 등급·상태 `Select`로 변경, 저장 시 스프레드 연산자로 해당 항목 인플레이스 수정

---

## 커밋

| 해시      | 내용                              |
| --------- | --------------------------------- |
| `c199482` | fix: 뱃지 색깔 global.css와 동일하게 변경 |
| `0548e02` | feat: 공지사항 추가 및 수정 추가  |
| `70d295d` | feat: 통계분석 페이지 추가        |
| `09e89a9` | feat: 회원관리 상세정보 및 수정 추가 |
