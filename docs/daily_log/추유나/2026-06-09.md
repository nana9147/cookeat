# 2026-06-09 작업 내역

## 오늘 반영된 주요 작업 요약

코드 리뷰 피드백 반영 및 상품 수정 페이지 추가를 진행

---

## 세부 작업

### 1. StatusBadge 판매 상태 불일치 수정

- **작업**: `StatusBadge.tsx ` 수정
- **내용**:
  - `types/seller/product.ts`의 `ProductStatus`에는 `판매대기/판매중/품절/판매종료` 4가지가 정의되어 있었으나
  - `StatusBadge`에는 `판매중/품절/판매종료` 3가지만 명시되어 있어 타입 불일치 발생
  - `StatusBadge`에 `판매대기` 상태 추가

### 2. 코드 리뷰 피드백 반영 - ImageUploadField/DescriptionEditor 폼 상태 미연결 수정

- **작업**: `ImageUploadField.tsx`, `DescriptionEditor.tsx`, `index.tsx`, `types/seller/product.ts` 수정
- **내용**:
  - `ImageUploadField` 내부 `useState` 제거, `data`/`onChange` props로 폼 상태 연결
  - `DescriptionEditor` `data`/`onChange` props 추가, `onUpdate`로 폼 상태 연결
  - `ProductImageItem`, `ImageUploadData`, `ImageUploadFieldProps` 타입 추가
  - `DescriptionData`, `DescriptionEditorProps` 타입 추가
  - `ProductFormData`에 `images`, `description` 섹션 추가
  - `ImageFile` 인터페이스 제거 (→ `ProductImageItem`으로 대체)

<<<<<<< HEAD
=======
### 3. 상품 수정 페이지 추가

- **작업**: `app/seller/products/[id]/edit/page.tsx` 생성
- **내용**:
  - `ProductForm mode="edit"` 적용
  - Next.js 15 `params` async 처리
  - 목데이터로 기존 상품 데이터 표시
  - `FormActionButtons` 등록하기/수정하기 텍스트 분기 적용
  - 할인 설정 `showDiscount` 초기값 `discountType` 기반으로 수정
  - 추후 API 연동 시 `initialData`로 기존 상품 데이터 넘길 예정

### 4. 상품 관리 페이지 기능 추가

- **작업**: `ProductTable.tsx`, `products/page.tsx` 수정
- **내용**:
  - 페이지네이션 추가 (7개 이상 시 `...` 처리, 이전/다음 버튼)
  - 페이지 1개일 때 페이지네이션 미표시
  - 빈 상태 UI 추가 (상품 없을 때 "상품이 없어요" 표시)
  - 전체 상품 수 표시 추가

### 5. FilterTabs 범용 컴포넌트로 리팩토링

- **작업**: `FilterTabs.tsx` 수정
- **내용**:
  - 기존 상품 관리 전용 → 제네릭 기반 범용 컴포넌트로 변경
  - `<T extends string>` 제네릭 적용으로 어떤 타입이든 사용 가능
  - `StatusBadge`에 `OrderStatus` 추가 (주문 관리 페이지 대비)
  - `products/page.tsx` FilterTabs 범용 컴포넌트로 교체

### 6. 배송 방법 택배 단일화

- **작업**: `ShippingSection.tsx`, `types/seller/shipping.ts` 수정
- **내용**:
  - 배송 방법 선택 UI 제거 (택배로 단일화)
  - `deliveryMethod` 필드 `ShippingData`에서 제거
  - `CourierCode` 타입 `shipping.ts`로 이동

### 7. 주문 관리 페이지 작업 시작 (WIP)

- **작업**: `types/seller/order.ts`, `app/seller/orders/page.tsx` 생성
- **내용**:
  - `OrderStatus`, `PaymentMethod`, `OrderStatusFilter`, `CourierCode` 타입 정의
  - `Order`, `OrderProduct`, `PaymentInfo`, `DeliveryInfo`, `OrderDetail` 인터페이스 정의
  - 주문 현황 카드 4개 구현 (결제완료/배송준비중/배송중/배송완료/취소·환불)
  - 카드 클릭 시 해당 상태로 필터링, 재클릭 시 필터 해제

>>>>>>> e1108c1 (docs: 2026-06-09 데이로그 추가)
---

## 커밋

<<<<<<< HEAD
| 해시      | 내용                                                                         |
| --------- | ---------------------------------------------------------------------------- |
| `86650ff` | fix: Product 타입 store 필드 선택값으로 변경 및 mock 데이터 타입 불일치 수정 |
| `206012a` | fix: ImageUploadField/DescriptionEditor 폼 상태 미연결 이슈 수정             |
=======
| 해시      | 내용                                                                           |
| --------- | ------------------------------------------------------------------------------ |
| `86650ff` | fix: Product 타입 store 필드 선택값으로 변경 및 mock 데이터 타입 불일치 수정   |
| `206012a` | fix: ImageUploadField/DescriptionEditor 폼 상태 미연결 이슈 수정               |
| `86193ab` | feat: 상품 수정 페이지 추가 (목데이터 적용)                                    |
| `9dca270` | feat: 상품관리 페이지 기능 추가                                                |
| `f49620a` | refactor: FilterTabs 범용 제네릭 컴포넌트로 변경, StatusBadge OrderStatus 추가 |
| `d1b6b25` | refactor: 배송 방법 택배 단일화로 deliveryMethod 필드 및 UI 제거               |
| `2d24a7d` | refactor: 상품 관리 페이지 FilterTabs 범용 컴포넌트로 교체 및 대시보드 수정    |
| `c2782e1` | feat: 주문 관리 페이지 추가 (현황 카드) - WIP                                  |
>>>>>>> e1108c1 (docs: 2026-06-09 데이로그 추가)
