# 2026-06-11 작업 내역

## 오늘 반영된 주요 작업 요약

주소 관리 페이지 추가 및 카카오 우편번호 연동, 배송 템플릿 관리 페이지 추가

---

## 세부 작업

### 1. 주소 관리 타입 확장

- **작업**: `types/seller/shipping.ts` 수정
- **내용**:
  - `AddressItem`에 `type`, `isDefault` 필드 추가
  - `AddressType` 타입 추가 (`'출고지' | '반품지'`)
  - `AddressFormType` 타입 추가 (`'등록' | '수정'`)
  - `AddressFormProps` 인터페이스 추가

### 2. StatusBadge 주소 상태 추가

- **작업**: `StatusBadge.tsx` 수정
- **내용**:
  - `AddressBadgeType` 타입 추가 (`기본출고지 | 기본반품지`)
  - `StatusBadge` props 타입에 `AddressBadgeType` 추가

### 3. shadcn 컴포넌트 추가

- **작업**: `tabs.tsx`, `alert-dialog.tsx` 추가
- **내용**:
  - `npx shadcn@latest add tabs`
  - `npx shadcn@latest add alert-dialog`

### 4. 주소 관리 컴포넌트 추가

- **작업**: `app/seller/components/Shipping/` 하위 컴포넌트 추가
- **내용**:
  - `AddressCard.tsx` 추가
    - 주소 카드 UI
    - 수정/삭제 버튼
    - 삭제 시 `AlertDialog`로 확인
    - 기본 주소 뱃지 표시
  - `AddressForm.tsx` 추가
    - 등록/수정 모드 분기
    - 카카오 우편번호 연동 (`react-daum-postcode`)
    - 우편번호 검색창 인라인 렌더링
    - `useEffect`로 수정 시 기존 데이터 폼에 주입
  - `AddressList.tsx` 추가
    - 출고지/반품지 탭 분리 (`Tabs` 컴포넌트)
    - 빈 상태 UI
    - 등록/수정/삭제 상태 관리
    - 기본 주소 설정 시 동일 타입 기존 기본 주소 해제 처리

### 5. 주소 관리 페이지 추가

- **작업**: `app/seller/shipping/address/page.tsx` 생성
- **내용**:
  - `AddressList` 컴포넌트 연결

### 6. 배송 템플릿 타입 정의 및 공통 컴포넌트 업데이트

- **작업**: `types/seller/shipping.ts`, `StatusBadge.tsx` 수정, `radio-group.tsx` 추가
- **내용**:
  - `ShippingTemplate` 인터페이스 추가
  - `ShippingTemplateFormProps`, `ShippingTemplateTableProps` 인터페이스 추가
  - `FormType` 타입으로 통일 (`AddressFormType` 대체)
  - `StatusBadge`에 `ShippingFeeType` 및 `'기본값'` 추가
  - `npx shadcn@latest add radio-group`

### 7. 배송 템플릿 관리 기능 구현

- **작업**: `app/seller/components/Template/` 하위 컴포넌트 추가
- **내용**:
  - `ShippingTemplateTable.tsx` 추가
    - 라디오버튼으로 기본 템플릿 선택
    - 행 클릭으로 라디오버튼 선택
    - 기본값 저장 시 AlertDialog 확인
    - 취소 시 기존 기본값으로 복원
    - 삭제 AlertDialog 적용
  - `ShippingTemplateForm.tsx` 추가
    - 등록/수정 모드 분기
    - 배송비 유형(무료/유료/조건부 무료) 조건부 렌더링
    - 주소록에서 출고지/반품지 선택
    - 유효성 검사
  - `TemplateList.tsx` 추가
    - 배송 템플릿 / 반품규정 템플릿 탭 분리
    - 등록/수정/삭제/기본값 상태 관리
  - `app/seller/shipping/templates/page.tsx` 생성

### 8. 배송관리 서브메뉴 추가

- **작업**: `SellerSidebar.tsx`, `SellerMobileNav.tsx` 수정
- **내용**:
  - 배송관리 하위 서브메뉴 추가 (배송 처리 / 주소 관리 / 템플릿 관리)
  - 모바일 네비게이션 서브메뉴 렌더링 추가

  ### 9. 반품규정 템플릿 타입 정의 추가

- **작업**: `types/seller/shipping.ts` 수정
- **내용**:
  - `ReturnPolicyContent` 인터페이스 추가
  - `ReturnPolicy` 인터페이스 추가
  - `NonReturnReason` 타입 추가
  - `ReturnPolicyFormProps`, `ReturnPolicyTableProps` 인터페이스 추가

### 10. 반품규정 템플릿 관리 기능 구현

- **작업**: `app/seller/components/Template/` 하위 컴포넌트 추가
- **내용**:
  - `ReturnPolicyTable.tsx` 추가
    - 라디오버튼 기본값 선택 및 저장
    - 행 클릭으로 라디오버튼 선택
    - 삭제 AlertDialog 적용
    - `useEffect`로 기본값 동기화
  - `ReturnPolicyForm.tsx` 추가
    - 반품 가능 기간 / 환불 처리 기간 직접 입력
    - 판매자 귀책 자동 적용
    - 반품 불가 사유 체크박스 (다중 선택)
    - 유효성 검사
  - `TemplateList.tsx` 반품규정 탭 연결
  - `ShippingTemplateTable.tsx` `useEffect` 버그 수정

---

## 커밋

| 해시      | 내용                                                  |
| --------- | ----------------------------------------------------- |
| `146d60f` | feat: 주소 관리 기반 타입 및 공통 컴포넌트 추가       |
| `76c4f63` | feat: 주소 관리 페이지 및 컴포넌트 추가               |
| `cda6db3` | feat: 배송 템플릿 타입 정의 및 공통 컴포넌트 업데이트 |
| `65f8eea` | feat: 배송 템플릿 관리 기능 구현                      |
| `bcc6cc8` | feat: 배송관리 서브메뉴 추가                          |
| `8449125` | feat: 반품규정 템플릿 타입 정의 추가                  |
| `1e758d6` | feat: 반품규정 템플릿 관리 기능 구현                  |
