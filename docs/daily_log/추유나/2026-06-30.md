# 2026-06-30 작업 내역

## 오늘 반영된 주요 작업 요약

Seller 코드 이슈 전반을 수정
보안(PG 환불 미연동·입력 검증), 데이터 정합성(Storage 누수·syncOrderStatus·롤백), 성능(인메모리 페이지네이션 → DB), 코드 일관성(응답 형식·Admin 읽기 전용 포털) 네 축으로 작업을 분류하여 진행

---

## 세부 작업

### 1. 환불 승인 시 PG 결제 취소 API 연동 (`ca7c883`)

- **파일**: `app/api/seller/orders/refunds/db.ts` — `approveRefund()`
- **내용**:
  - `orders` 테이블에서 `payment_method` / `payment_key` 조회 후 PG사 환불 API 호출
  - KakaoPay: `POST /v1/payments/{paymentKey}/cancel`, TossPay 동일 패턴 적용
  - PG 호출 실패 시 `refund_requests` 상태를 원복하는 보상 처리 추가

### 2. 환불 승인 중 이력/주문 동기화 실패 시 롤백 (`6cceff2`)

- **파일**: `app/api/seller/orders/refunds/db.ts`
- **내용**:
  - `logOrderItemStatusHistory()` 또는 `syncOrderStatusIfFullyRefunded()` 실패 시 `refund_requests.status`를 이전 상태로 롤백

### 3. 구매확정 항목 환불 승인 차단 (`03a7782`)

- **파일**: `app/api/seller/orders/refunds/db.ts`
- **내용**:
  - `settlement_items`에 이미 포함된 항목의 환불 승인 시도 시 400 에러 + 안내 메시지 반환

### 4. 환불/취소 거부 시 status 컬럼 갱신 누락 수정 (`94ca020`)

- **파일**: `app/api/seller/orders/refunds/db.ts` — `rejectRefund()`
- **내용**:
  - `update` 쿼리에 `status: refund.status === '취소요청' ? '취소거부' : '환불거부'` 추가

### 5. 상품 등록/수정 서버사이드 입력 검증 강화 (`83f5d26`)

- **파일**: `app/api/seller/products/route.ts`
- **내용**:
  - `status` 허용 enum 검사 (`'판매중'|'품절'|'판매종료'|'숨김'`)
  - `price > 0`, `stock >= 0` 범위 검사
  - `discountType`/`discountValue` 유효성 검사 (정률 0~100)
  - 이미지 타입(`jpeg/png/webp`) 및 크기(5MB) 제한

### 6. export 엔드포인트 limit 상한 추가 (`a408001`)

- **파일**: `app/api/seller/orders/export/route.ts`
- **내용**: `Math.min(limit, 1000)` 클램핑 추가

### 7. Storage 누수 방지 (`c3da22f`)

- **파일**: `app/api/seller/products/db.ts` — `createSellerProduct()` / `updateSellerProduct()`
- **내용**:
  - 업로드 성공 URL을 `uploadedUrls` 배열에 누적
  - catch 블록에서 DB 행 삭제 + `Promise.allSettled()`로 Storage 파일 일괄 삭제

### 8. 단건 배송 상태 변경 시 orders.status 동기화 (`d9057e7`)

- **파일**: `app/api/seller/shipping/orders/db.ts` — `updateShippingStatus()`
- **내용**: 함수 마지막에 `await syncOrderStatus(orderId)` 호출 추가

### 9. 정산 race condition 방지 및 일괄 처리 실패 상세 표시 (`0352790`)

- **파일**: `app/api/seller/settlements/db.ts` — `ensureSettlements()`
- **내용**:
  - 동시 요청 충돌(23505) catch 처리 강화
  - `bulkUpdateShippingTracking()` 부분 실패 항목 상세를 UI에 노출

### 10. 진행 중인 주문이 있는 상품 삭제 차단 (`1af7f64`)

- **파일**: `app/api/seller/products/[productId]/db.ts` — `deleteSellerProduct()`
- **내용**:
  - 삭제 전 `결제완료/배송준비/배송중` 상태 주문 존재 여부 확인
  - 존재 시 409 응답 + `'판매종료'`로 상태 변경 권장 안내

### 11. 주문/배송/환불/정산 DB 수준 페이지네이션 전환 (`881416a`, `8b4b37b`)

- **파일**: `orders/db.ts`, `shipping/orders/db.ts`, `refunds/db.ts`, `settlements/db.ts`
- **내용**:
  - 전체 조회 후 `.slice()` → Supabase `.range(from, to)` / `.limit().offset()` 교체

### 12. 판매자 API 응답 형식 통일 + 상품 검색 즉시 반영 (`dbd1fff`)

- **파일**: `me/route.ts` 등 기타 라우트
- **내용**:
  - 에러 응답 `{ success: false, error: '...' }` 형식으로 통일
  - 상품 목록 검색 트리거를 `onChange` 즉시 반영으로 변경

### 13. Admin 셀러 포털 읽기 전용 접근 구현 (`9182425` → `d806582`)

- **파일**: `lib/sellerContext.ts`, `app/seller/**`
- **내용**:
  - `requireSellerContext`에서 admin 여부 판별 → `isAdmin` 플래그 반환
  - admin 진입 시 `?sellerId=` 파라미터 필수화, 누락 시 에러 처리
  - 쓰기 버튼(등록·수정·삭제·승인 등) `isAdmin` 기준 조건부 숨김
  - 중복 toast 메시지 중복 방지

### 14. /api/seller/me admin 404 문제 수정 (`31d4e94`)

- **파일**: `app/api/seller/me/route.ts`
- **내용**: admin 계정은 `sellers` 테이블에 없으므로 `requireSeller` 대신 `requireSellerContext` 사용 + admin 분기 처리

### 15. 기타 코드 품질 개선 (`5dc6938`)

- `isLodding` → `isLoading` 오타 수정
- `useEffect` deps 배열 `fetchCounts` 누락 수정
- `STATUS_LABEL` 중복 선언 제거

---

## 커밋

| 해시      | 내용                                                                      |
| --------- | ------------------------------------------------------------------------- |
| `d806582` | feat: admin 셀러 포털 읽기 전용 UI 강화 — 쓰기 버튼 숨김 처리             |
| `5a64a8d` | fix: admin seller 초기화 및 중복 토스트 메시지 수정                       |
| `2949ebc` | fix: admin seller 포털 진입 시 sellerId 파라미터 누락 오류 수정           |
| `8b4b37b` | refactor: settlements DB 레벨 페이지네이션 적용 및 isAdmin 선언 순서 수정 |
| `ca7c883` | fix: 환불 승인 시 PG 결제 취소 API 연동                                   |
| `31d4e94` | fix: /api/seller/me admin 접근 시 404 반환 문제 수정                      |
| `9182425` | feat: admin 계정 seller 포털 읽기 전용 접근 구현                          |
| `94ca020` | fix: 환불/취소 거부 시 status 컬럼 갱신 누락 수정                         |
| `83f5d26` | fix: 상품 등록/수정 서버사이드 입력 검증 강화                             |
| `a408001` | fix: export 엔드포인트 limit 상한 1000 제한 추가                          |
| `c3da22f` | fix: Storage 누수 방지 — 상품 생성/수정 실패 시 업로드 파일 정리          |
| `6cceff2` | fix: 환불 승인 중 이력 기록/주문 동기화 실패 시 refund_requests 롤백      |
| `0352790` | fix: 정산 동시 요청 race condition 방지 및 일괄 처리 실패 상세 표시       |
| `03a7782` | feat: 구매확정 항목 환불 승인 차단                                        |
| `d9057e7` | fix: 단건 배송 상태 변경 시 orders.status 동기화 누락 수정                |
| `1af7f64` | feat: 진행 중인 주문이 있는 상품 삭제 차단                                |
| `dbd1fff` | fix: 판매자 API 응답 형식 통일 및 상품 목록 즉시 검색 개선                |
| `881416a` | refactor: 판매자 주문/배송/환불/정산 DB 수준 페이지네이션으로 전환        |
| `5dc6938` | refactor: seller 코드 품질 개선                                           |
